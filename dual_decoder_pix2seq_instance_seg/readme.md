Instance segmentation with Dual Decoder where logic is to generate boxes with Decoder 1 and pass the output to Decoder 2 to generate masks (as polygons)
Works well, sequence augmentation is also added.

